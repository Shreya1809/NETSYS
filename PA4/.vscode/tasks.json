{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make clean all",
            "type": "shell",
            "command": "cd ${workspaceFolder}/client/DFC/ && make clean && make && cd ${workspaceFolder}/server/ && make clean && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}